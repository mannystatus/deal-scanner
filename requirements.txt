sqlalchemy>=2.0
pydantic>=2.5
httpx>=0.27
python-dotenv>=1.0
feedparser>=6.0

# social_post.py — X (Twitter) posting; Instagram/Threads use plain httpx calls
tweepy>=4.14

# API
fastapi>=0.110
uvicorn[standard]>=0.27

# Postgres driver (Neon uses standard postgresql:// URLs)
psycopg2-binary>=2.9

# Web push notifications (worker.py sends, api.py serves the public key)
pywebpush>=2.0

# Dev
pytest>=8.0
