// Runtime config for the frontend. Edit this file to point at a different API.
// Render serves it with a 60-second cache (see render.yaml headers).
//
// Locally: just open frontend/index.html directly, or run
//   python -m http.server 5173 --directory frontend

window.API_BASE = "https://deal-scanner-api.onrender.com";
